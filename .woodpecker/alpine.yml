pipeline:
    build:
        image: codeberg.org/generic-pers0n/tenacity-alpine:latest
        commands:
            - mkdir build
            - cmake -S . -B build
            - cmake --build ./build
        when:
            branch: main
            event: pull_request
