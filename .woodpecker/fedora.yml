pipeline:
    build:
        image: gperson/tenacity-fedora
        commands:
            - mkdir build
            - cd build
            - cmake -G Ninja .. -DMIDI=Off
            - cmake --build .
        when:
            branch: main
            event: pull_request
